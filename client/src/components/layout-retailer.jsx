import React from "react";
import { Outlet } from "react-router-dom";

const Layout_retailer = () => {
    return (
        <div>
            <Outlet />
        </div>
    );
};

export default Layout_retailer;
